<div class="container">
    <h3>Welcome, {{user.firstName}}.</h3>
    <h3>{{u.userName}}</h3>
    {{#each friends}}
    <a style="color: white" href="/chat/{{roomId}}">
        <div class="card-panel teal lighten-2">{{userName}}</div>
    </a>
    {{/each}}
</div>